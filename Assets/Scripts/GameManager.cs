using UnityEngine;

public class GameManager : MonoBehaviour
{
    public GameObject directionalLight;

    public void Update()
    {
        //directionalLight.transform.rotation = Camera.main.transform.rotation;
    }
}
